# =============================================================================
# mseONE PoC API - Environment Configuration Template
# =============================================================================
# Copy this file to .env and replace the example values with your actual configuration

# =============================================================================
# COSMOS DB CONFIGURATION (Required)
# =============================================================================
# Your Cosmos DB account endpoint URL
COSMOS_URI=https://your-cosmos-account.documents.azure.com:443/

# Cosmos DB primary key (get from Azure Portal > Cosmos DB > Keys)
COSMOS_KEY=your-cosmos-primary-key-here

# Database name (will be created if it doesn't exist)
COSMOS_DB=projectsdb

# Container name (will be created if it doesn't exist)
COSMOS_CONTAINER=projects

# =============================================================================
# AZURE STORAGE CONFIGURATION (Optional - for API result logging)
# =============================================================================
# Azure Storage account name
STORAGE_ACCOUNT=your-storage-account-name

# Azure Storage account access key (get from Azure Portal > Storage Account > Access Keys)
STORAGE_KEY=your-storage-account-key

# =============================================================================
# APPLICATION ENVIRONMENT
# =============================================================================
# Environment mode: 'local' for development, 'azure' for production
APP_ENV=local

# Local development bearer token (used when APP_ENV=local)
LOCAL_DEV_TOKEN=devtoken123

# =============================================================================
# AZURE AD CONFIGURATION (Only required when APP_ENV=azure)
# =============================================================================
# Azure Active Directory tenant ID
AZURE_TENANT_ID=your-azure-tenant-id

# Azure AD application audience/client ID (format: api://your-app-id)
AZURE_AUDIENCE=api://your-azure-app-id

# =============================================================================
# DEVELOPMENT SETTINGS (Optional)
# =============================================================================
# Enable debug mode (true/false)
DEBUG=true

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Development server port (optional, defaults handled by uvicorn)
PORT=8001

# =============================================================================
# QUICK SETUP GUIDE
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Replace placeholder values with your actual Azure resource details
# 3. For local development, keep APP_ENV=local
# 4. Run: python setup_cosmos.py (to initialize database)
# 5. Start API: uvicorn api.main:app --host 0.0.0.0 --port 8001 --reload
#
# Get Azure resource details from:
# - Cosmos DB: Azure Portal > Cosmos DB > Keys
# - Storage: Azure Portal > Storage Account > Access Keys  
# - Azure AD: Azure Portal > App Registrations > Your App
# =============================================================================